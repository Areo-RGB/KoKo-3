{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Delete Empty Folders",
      "type": "shell",
  "command": "folders=$(find . -type d -empty -not -path './.git*'); if [ -n \"$folders\" ]; then echo 'Folders deleted:'; echo \"$folders\"; find . -type d -empty -not -path './.git*' -delete; else echo 'No empty folders found.'; fi",
      "group": {
        "kind": "build",
        "isDefault": false
      },
      "presentation": {
        "reveal": "always"
      },
      "problemMatcher": []
    }
  ]
}
